node_modules
.expo
dist
web-build
coverage
android
ios
package-lock.json

# Generado por drizzle-kit: se regenera en cada `db:generate` y volvería a
# desformatearse, dejando CI en rojo sin que nadie haya tocado nada.
drizzle/meta/
drizzle/migrations.js

# El servidor se formatea con las mismas reglas, pero su build y sus
# dependencias no.
servidor/node_modules
servidor/dist
servidor/drizzle
